twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

    globals:
        max_tasks_on_homepage: '%max_tasks_on_homepage%'

    form_themes:
        - 'bootstrap_3_horizontal_layout.html.twig'
